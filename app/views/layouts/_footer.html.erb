<footer class="container" style="width: 100%; background-color: orange;">
	<div class="col-md-5"></div>
	<div class="col-md-5" style="margin-top: 20px;">
		<%= link_to 'stock gourmet', root_path, class: "navbar-brand active" %>
	</div>
	<div class="col-md-2" style="margin-top: 35px;">
		<a href="#top">Topへ戻る</a>
	</div>
	<% if user_signed_in? %>
	<div>
		<%= link_to "退会の方はこちらへ", users_path(@user.id) %>
	</div>
	<% end %>
</footer>